spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fruit?characterEncoding=utf8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mapper/*.xml
server:
  port: 8181

  # ?characterEncoding=utf8 允许向映射向数据中文
